import Title from "../../components/Title/Title";

function Header() {
    const titulo = 'Juan Sebastian'
    return <>
        <Title text={titulo} />
        <Title text={titulo} />
        <Title text={titulo} />
        <Title text={titulo} />
        <Title text={titulo} />
    </>
}

export default Header;